source 'https://rubygems.org'

gem 'bundler'
gem 'rake'

gem 'lotusrb',       '0.3.1'
gem 'lotus-model',   '~> 0.3'

group :development, :test do
  gem 'sqlite3'
end

group :test do
  gem 'minitest'
  gem 'capybara'
end

group :production do
  # gem 'puma'
end
